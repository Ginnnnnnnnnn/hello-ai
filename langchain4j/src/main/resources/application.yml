server:
  port: 8001
  servlet:
    context-path: /langchain4j
spring:
  application:
    name: langchain4j
  profiles:
    active: @profiles.active@
langchain4j:
  open-ai:
    chat-model:
      api-key: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
      model-name: qwen-plus
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
      log-requests: true
      log-responses: true