server:
  port: 8002

spring:
  application:
    name: mcp-weather-stdio
  main:
    web-application-type: none
    banner-mode: off
  ai:
    mcp:
      server:
        name: mcp-server
        version: 1.0.0
        stdio: true # 开启 stdio
        enabled: true
        type: SYNC

logging:
  level:
    root: OFF